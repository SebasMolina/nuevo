import React, { useState } from "react";
// importamos la app desde el appnavigation
import App from './src/AppNavigation'

/*definimos una constante diferente a App para que no exista errores
	con el appnavigation
*/

const Main = () => {
	return(
		<App/>
	)
}

export default Main;